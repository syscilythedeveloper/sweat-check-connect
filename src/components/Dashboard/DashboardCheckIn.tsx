/* eslint-disable @typescript-eslint/no-explicit-any */
//import { DashboardCheckInCard } from "@/types/checkIns";

import React from "react";

const DashboardCheckIn = (challenge: any) => {
  return <div>{JSON.stringify(challenge)}</div>;
};

export default DashboardCheckIn;
